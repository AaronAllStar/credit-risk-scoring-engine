services:
  credit-risk-engine:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: credit_risk_engine
    ports:
      - "3000:3000"
    environment:
      - RUST_LOG=info
      - PORT=3000
    restart: unless-stopped
    volumes:
      - ./data:/app/data:ro
